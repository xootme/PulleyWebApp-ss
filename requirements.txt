flask>=3.0
# cct_common is vendored directly into this repo (see sync_cct_common.py) —
# it's a private GitHub repo, so it can't be installed on Render via an
# editable sibling path or a git URL. No requirements.txt line needed: a
# package directory next to app.py is on sys.path by default.
flask-compress>=1.14
pillow>=10.0
cairosvg>=2.7
ezdxf>=1.3
gunicorn
trimesh>=4.0
shapely>=2.0
scipy>=1.10
numpy>=1.24
manifold3d>=3.0
sendgrid>=6.9
pytest-benchmark>=4.0
psutil>=5.9
requests>=2.31
